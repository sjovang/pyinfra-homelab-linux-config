# Homelab config with pyinfra

Use [pyinfra](https://docs.pyinfra.com/en/2.x/index.html#) to manage configuration (for arch linux based systems for now) in homelabs

## Examples

Run with `pyinfra inventory.py deploy.py --dry` to verify proposed changes and `pyinfra inventory.py deploy.py` to apply config